# 📊 Universal Logging System (ULS)

A robust, centralized dashboard for managing application logs. This system allows for submitting log entries, viewing them in a high-density table, and exporting them as CSV files for further analysis.

## ✨ Features

- **Centralized Dashboard**: Unified view for all your application logs.
- **API Key Security**: Secure endpoints using custom `X-API-KEY` header validation.
- **Log Submission**: Easy-to-use form to submit logs from the browser.
- **High-Density Table**: Optimized 4-column layout (Timestamp, Service, Message, Level) with forced desktop view for maximum data visibility.
- **CSV Export**: Downloadable CSV reports with automatic server-side archiving in the `exports/` folder.
- **JSON Storage**: Lightweight and portable data storage using local JSON files.

## 🛠 Tech Stack

- **Backend**: Python (Flask, Flask-CORS)
- **Frontend**: Vanilla HTML5, CSS3, ES6+ JavaScript
- **Storage**: JSON File System (`logs.json`, `api_keys.json`)

## 🚀 Getting Started

### 1. Prerequisites
Ensure you have Python 3.8+ installed on your system.

### 2. Install Dependencies
Run the following command to install the required Python packages:
```bash
pip install flask flask-cors
```

### 3. Run the Backend
Start the Flask server by running:
```bash
python app.py
```
The API will be available at `http://localhost:5000`.

### 4. Open the Frontend
Simply open `index.html` in your favorite web browser.

## 🔌 API Documentation

| Endpoint | Method | Description | Auth Required |
|----------|--------|-------------|---------------|
| `/api/health` | `GET` | System health check | No |
| `/api/key/generate` | `POST` | Generate a new static API Key | No |
| `/api/logs` | `GET` | Retrieve all log entries | Yes |
| `/api/logs` | `POST` | Submit a new log entry | Yes |
| `/api/logs/export` | `GET` | Export logs to CSV (Saves on server & Downloads) | Yes |
| `/api/logs/clear` | `POST` | Wipe all logs from the system | Yes |

*Note: Authenticated routes require the `X-API-KEY` header.*

## 📁 File Structure

```text
sri/
├── app.py              # Flask Backend serving the API
├── index.html          # Main Dashboard layout
├── style.css           # Modern dark-theme styling
├── script.js           # Frontend logic and API integration
├── logs.json           # Log entry storage
├── api_keys.json       # Generated API keys storage
└── exports/            # Server-side CSV archives folder
```

## 🔐 Security
The dashboard automatically handles API key generation on first run. Keys are stored in `api_keys.json` and persist across sessions via browser `localStorage`.

---
*Generated by Universal Logging System v1.0.0*
